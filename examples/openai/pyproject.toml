[project]
name = "openai-mcp-example"
version = "0.1.11"
description = "OpenAI MCP example"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "modbus-mcp>=0.3.2",
    "openai>=2.40.0",
    "pydantic-settings>=2.10.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "ruff>=0.15.15",
]
